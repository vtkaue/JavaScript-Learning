/*
       ARRAY - MÉTODOS
*/

// push = adciona um itme no array

// length = quantos itens tem no array

// sort = organiza por ordem alfabetica

// delete = deleta um item do nosso array(deixa vazio)

/* splice (unir / ligar) = Permite tirar itens do nosso array (exatamente onde queremos), ou coloca itens (exatamente onde queremos). 
- apartir de que item quero começar a tirar

*/


// slice (fatiar) = tira itens do array(criando um novo)

// pop = Tira o último item do array

// shift = tira o primeiro item do array

const students = ['joão', 'Maria', 'Pedro', 'Carla', 'baka', 'aris']

const newStudents = students.slice(3)
console.log(newStudents)